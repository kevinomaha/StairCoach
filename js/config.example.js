/**
 * Configuration template for StairCoach
 * 
 * Copy this file to config.js and fill in your own API keys.
 * The config.js file is in .gitignore and won't be committed to version control.
 */

export const config = {
  // Jamendo API client_id 
  // Get a free key at https://www.jamendo.com/developer
  // Keep this secret and don't share publicly
  jamendoClientId: "YOUR_JAMENDO_CLIENT_ID_HERE"
};
